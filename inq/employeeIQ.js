const { prompt } = require('inquirer');

const addEmployee = () => {

    prompt([
        {

        }
    ]).then(results => {

    });

}

const remEmployee = () => {

    prompt([
        {
            
        }
    ]).then(results => {

    });
}